import { Device } from '../../types/devices';
import styles from './Equipment.module.scss'

interface EquipmentListProps {
    // items: Partial<Device>[]
}
const EquipmentList = ({  }: EquipmentListProps) => {
    return (
        <div>
           888
        </div>
    );
};

export default EquipmentList;