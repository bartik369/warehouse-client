name: Deploy Frontend to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозитория
        uses: actions/checkout@v3

      - name: Упаковка frontend кода
        run: tar -czf frontend.tar.gz . --exclude=node_modules

      - name: Отправка архива на сервер
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          source: "frontend.tar.gz"
          target: "/srv/apps/"

      - name: Отправка .env на сервер
        run: echo "${{ secrets.ENV_PROD }}" > .env

      - name: Копирование .env на сервер
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          source: ".env"
          target: "/srv/apps/frontend/"

      - name: Распаковка и перезапуск контейнеров
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          command_timeout: 20m
          script: |
            echo "Распаковываем frontend в нужную директорию"
            mkdir -p /srv/apps/frontend
            tar -xzf /srv/apps/frontend.tar.gz -C /srv/apps/frontend

            echo "Удаляем архив"
            rm /srv/apps/frontend.tar.gz

            echo "Переходим в корень проекта"
            cd /srv/apps

            echo "Пересобираем и перезапускаем контейнеры"
            docker compose down
            docker compose build --no-cache
            docker compose up -d
