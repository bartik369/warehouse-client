import React, {FC} from 'react';
import style from './Logo.module.scss';

const Logo:FC = () => {
    return (
        <div className={style.logo}>
            LOGO
        </div>
    );
};

export default Logo;